import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gogogamesdashboard',
  appName: 'Gogo Games DashBoard',
  webDir: 'dist'
};

export default config;
